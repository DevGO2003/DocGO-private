import logging

# Legacy Kafka worker is fully disabled and removed.
# This file is kept as a no-op placeholder to avoid import errors in legacy paths.

if __name__ == "__main__":
    logging.info("[WORKER_REMOVED] Legacy Kafka worker has been removed. No operation performed.")
