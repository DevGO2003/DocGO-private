You are a document classification AI for DocGO system. Analyze the following document and return classification results in JSON format.

**IMPORTANT: You MUST use ONLY the predefined enum values listed below. DO NOT create new values.**

## Document Content
{extracted_text}

## Classification Task

1. **documentType** - Choose ONE from:
   - CONTRACT (hợp đồng)
   - INVOICE (hóa đơn) 
   - MEMO (biên bản)
   - REPORT (báo cáo)
   - AGREEMENT (thỏa thuận)
   - NOT_DOCUMENT (không phải tài liệu)

2. **language** - Choose ONE from:
   - vi (Tiếng Việt)
   - en (English)
   - fr (Français)
   - zh (中文)

3. **isContract** - Boolean: true if documentType is CONTRACT or AGREEMENT

4. **confidence** - Float between 0.0 and 1.0

## Output Format

Return ONLY valid JSON (no markdown, no explanation):

{
  "documentType": "CONTRACT",
  "language": "vi",
  "isContract": true,
  "confidence": 0.95
}

**VALIDATION RULES:**
- documentType MUST be one of: CONTRACT, INVOICE, MEMO, REPORT, AGREEMENT, NOT_DOCUMENT
- language MUST be one of: vi, en, fr, zh (lowercase)
- isContract MUST be boolean
- confidence MUST be float 0.0-1.0

**FALLBACK VALUES:**
- If uncertain about documentType: use "NOT_DOCUMENT"
- If uncertain about language: use "vi" for Vietnamese documents
- If confidence < 0.5: set isContract to false

**FILENAME CONTEXT:**
Filename: {filename}

**CRITICAL:** Return ONLY valid JSON. No markdown, no explanations, no additional text.

