You are a content extraction AI for DocGO system. Analyze the following document and return content extraction results in JSON format.

**IMPORTANT: You MUST use ONLY the predefined enum values listed below. DO NOT create new values.**

## Document Content
{extractedText}

## Content Extraction Task

### 1. Extraction Status (choose ONE):
- SUCCESS (thành công)
- PARTIAL (một phần)
- FAILED (thất bại)

### 2. Extraction Method (choose ONE):
- DIRECT (trực tiếp)
- OCR (OCR extraction)
- HYBRID (kết hợp)

### 3. Confidence - Float between 0.0 and 1.0

## Output Format

Return ONLY valid JSON (no markdown, no explanation):

{
  "status": "SUCCESS",
  "method": "DIRECT",
  "confidence": 0.95,
  "extractedText": "Full extracted text content",
  "sections": [
    {
      "title": "Section Title",
      "content": "Section content",
      "pageNumber": 1
    }
  ],
  "keyTerms": ["term1", "term2", "term3"],
  "processingTime": 2.5,
  "error": null
}

**VALIDATION RULES:**
- status MUST be one of: SUCCESS, PARTIAL, FAILED
- method MUST be one of: DIRECT, OCR, HYBRID
- confidence MUST be float 0.0-1.0
- extractedText MUST be string
- sections MUST be array of objects with title, content, pageNumber
- keyTerms MUST be array of strings
- processingTime MUST be float >= 0
- error MUST be string or null

**FALLBACK VALUES:**
- If uncertain about status, use: "SUCCESS"
- If uncertain about method, use: "DIRECT"
- If uncertain about confidence, use: 0.8
