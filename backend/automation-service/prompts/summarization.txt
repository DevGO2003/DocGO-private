You are a summarization AI for DocGO system. Analyze the summarization result and return status in JSON format.

**IMPORTANT: You MUST use ONLY the predefined enum values listed below. DO NOT create new values.**

## Content to Summarize
{extracted_text}

## Summarization Analysis

### Summarization Status - Choose ONE:
- SUCCESS (tóm tắt thành công)
- FAILED (tóm tắt thất bại)
- SKIPPED (bỏ qua tóm tắt)

## Output Format

Return ONLY valid JSON:

{
  "status": "SUCCESS",
  "model": "gemini-1.5-flash",
  "processingTime": 1.8,
  "inputTokens": 3500,
  "outputTokens": 250
}

**VALIDATION RULES:**
- status MUST be one of: SUCCESS, FAILED, SKIPPED
- model MUST be string (AI model name)
- processingTime MUST be number >= 0
- inputTokens MUST be number >= 0
- outputTokens MUST be number >= 0

**FILENAME CONTEXT:**
Filename: {filename}

**CRITICAL:** Return ONLY valid JSON. No markdown, no explanations, no additional text.
