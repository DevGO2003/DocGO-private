spring.application.name=auth-service
# Server
server.port=8001

# MongoDB Atlas Configuration
spring.data.mongodb.uri=${MONGODB_ATLAS_URI:mongodb+srv://root:sapassword@devgo-docgo-cluster0.hsudzga.mongodb.net/docgo_auth?retryWrites=true&w=majority&appName=devgo-docgo-cluster0}
spring.data.mongodb.database=${MONGODB_DATABASE:docgo_auth}
spring.data.mongodb.auto-index-creation=true

# Legacy MariaDB (for backward compatibility) - DISABLED for local testing
# spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mariadb://localhost:3306/docgo}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME:docgo_user}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:docgo_password}
# spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# JPA/Hibernate Configuration (for legacy entities) - DISABLED for local testing
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# Logging
logging.level.org.springframework=INFO
logging.level.com.devgo2003.docgo.auth_service=DEBUG

# Swagger/OpenAPI
springdoc.swagger-ui.path=/docs
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.config-url=/v3/api-docs/swagger-config
springdoc.swagger-ui.url=/v3/api-docs

# Redirect configuration
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=true

# JWT Configuration
security.jwt.base64-secret=${JWT_SECRET:VGhpcy1pcy1hLXN1cGVyLWxvbmcgc2VjcmV0LXBsZWFzZS1vdmVycmlkZS1pbi1lbmYu}
security.jwt.access-ttl-seconds=${JWT_ACCESS_EXPIRATION:3600}
security.jwt.refresh-ttl-seconds=${JWT_REFRESH_EXPIRATION:2592000}

# Redis Cloud Configuration
spring.data.redis.host=${REDIS_CLOUD_HOST:localhost}
spring.data.redis.port=${REDIS_CLOUD_PORT:6379}
spring.data.redis.password=${REDIS_CLOUD_PASSWORD:}
spring.data.redis.database=${REDIS_DB:0}
spring.data.redis.timeout=${REDIS_TIMEOUT:2000}ms
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0
auth.blacklist.provider=redis # values: redis|memory

# Spring Boot DevTools Configuration
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
spring.devtools.livereload.port=35729
spring.devtools.restart.poll-interval=1000
spring.devtools.restart.quiet-period=400
spring.devtools.restart.exclude=static/**,public/**,resources/**,META-INF/maven/**,META-INF/resources/**

# OAuth2 Client (Google) - enabled with environment variables
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
spring.security.oauth2.client.registration.google.scope=openid,profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/v2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v2/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=id