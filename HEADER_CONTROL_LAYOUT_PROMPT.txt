TASK: Tạo HeaderControlLayout System với Pattern Extend cho các Features

REQUIREMENTS:
1. Tạo HeaderControlLayout global làm base layout
2. Mỗi feature sẽ extend thành specific layout (UploadHeaderLayout, DashboardHeaderLayout, etc.)
3. Sử dụng UIComponents có sẵn, không tạo mới
4. Hạn chế Tailwind classes, ưu tiên UIComponents
5. Đổi tên file từ HeaderPanel thành HeaderControlLayout

STRUCTURE:
```
frontend/webapp/src/shared/layouts/
├── HeaderControlLayout/
│   ├── HeaderControlLayout.tsx (base layout)
│   ├── index.ts
│   └── types.ts
├── UploadHeaderLayout/
│   ├── UploadHeaderLayout.tsx (extends HeaderControlLayout)
│   └── index.ts
├── DashboardHeaderLayout/
│   ├── DashboardHeaderLayout.tsx (extends HeaderControlLayout)
│   └── index.ts
└── RepositoryHeaderLayout/
    ├── RepositoryHeaderLayout.tsx (extends HeaderControlLayout)
    └── index.ts
```

HEADER PANEL STRUCTURE:
```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           HEADER PANEL                                         │
├─────────────────────────────────────────────────────────────────────────────────┤
│ 🏠 > Breadcrumb > Path                                    [Right Actions] │
│ Title                                                                           │
│ Subtitle                                                                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│ [Primary Tabs] [Secondary Tabs]                                [More Actions] │
└─────────────────────────────────────────────────────────────────────────────────┘
```

FILES TO CREATE:
1. frontend/webapp/src/shared/layouts/HeaderControlLayout/HeaderControlLayout.tsx
2. frontend/webapp/src/shared/layouts/HeaderControlLayout/index.ts
3. frontend/webapp/src/shared/layouts/HeaderControlLayout/types.ts
4. frontend/webapp/src/shared/layouts/UploadHeaderLayout/UploadHeaderLayout.tsx
5. frontend/webapp/src/shared/layouts/UploadHeaderLayout/index.ts
6. frontend/webapp/src/shared/layouts/DashboardHeaderLayout/DashboardHeaderLayout.tsx
7. frontend/webapp/src/shared/layouts/DashboardHeaderLayout/index.ts
8. frontend/webapp/src/shared/layouts/RepositoryHeaderLayout/RepositoryHeaderLayout.tsx
9. frontend/webapp/src/shared/layouts/RepositoryHeaderLayout/index.ts

FILES TO RENAME:
1. frontend/webapp/src/shared/layouts/HeaderPanel/HeaderPanel.tsx → DELETE
2. frontend/webapp/src/shared/layouts/HeaderPanel/index.ts → DELETE

FILES TO UPDATE:
1. frontend/webapp/src/shared/layouts/index.ts (update exports)
2. Các file đang import HeaderPanel → update import

UI COMPONENTS TO USE:
- Button, Input, Select từ @shared/components/UIComponents
- Tabs từ @shared/components/UIComponents/Tabs
- Breadcrumb từ @shared/components/UIComponents/Breadcrumb
- Không tạo UI component mới

TAILWIND RESTRICTIONS:
- Chỉ dùng basic classes: flex, grid, p-, m-, text-, bg-
- Không dùng complex responsive classes
- Ưu tiên UIComponents thay vì custom Tailwind

PATTERN EXTEND:
```typescript
// Base Layout
export interface HeaderControlLayoutProps {
  title: string;
  subtitle?: string;
  breadcrumbs?: Breadcrumb[];
  rightActions?: React.ReactNode;
  primaryTabs?: React.ReactNode;
  secondaryTabs?: React.ReactNode;
  children?: React.ReactNode;
}

// Feature-specific Layout
export interface UploadHeaderLayoutProps extends HeaderControlLayoutProps {
  uploadActions?: React.ReactNode;
  fileFilters?: React.ReactNode;
}
```

IMPLEMENTATION STEPS:
1. Tạo HeaderControlLayout base với 2 vùng được chia bằng border
2. Tạo types cho props
3. Tạo feature-specific layouts extend từ base
4. Update exports trong index.ts
5. Update imports trong các file đang dùng HeaderPanel
6. Test với 1-2 features để đảm bảo hoạt động

PRIORITY: High
DEADLINE: Immediate

