# Lưu lại nội dung so sánh và yêu cầu AI thực hiện điều chỉnh tốt nhất

## Prompt

```
Dựa trên bảng kiểm tra mapping ContractSummaryPublished sau đây, hãy thực hiện các điều chỉnh cần thiết để đảm bảo tính nhất quán và đầy đủ dữ liệu:

## Bảng Kiểm Tra Mapping ContractSummaryPublished

| **AI Publish** | **Contract Consume** | **Database Schema** | **Kết Quả** | **Lý Do** |
|---|---|---|---|---|
| **EVENT HEADER** |||||
| `eventVersion: "v1"` | Đọc `event.get("eventVersion")` | Không lưu | ✅ OK | Chỉ dùng để routing, không cần persist |
| `eventType: "ContractSummaryPublished"` | Check `equals("ContractSummaryPublished")` | Không lưu | ✅ OK | Dùng để route đến handler đúng |
| `eventId: uuid` | Đọc `event.get("eventId")` | Không lưu | ✅ OK | Event tracking, không cần persist |
| `timestamp: ISO-8601` | Đọc `event.get("timestamp")` | Không lưu | ✅ OK | Event metadata, không cần persist |
| `source: "ai-processing-service"` | Đọc `event.get("source")` | Không lưu | ✅ OK | Biết nguồn event, không cần persist |
| `correlationId: uuid` | Đọc `event.get("correlationId")` | Không lưu | ✅ OK | Tracing, không cần persist |
| `actor.userId/userRole/ip` | Đọc `event.get("actor")` | Không lưu | ⚠️ PARTIAL | Có thể lưu audit trail sau |
| **CONTRACT DATA** |||||
| `data.contractNumber: "CTR-2024-001"` | Đọc nhưng không set | `Contract.contract_number` | ❌ MISSING | Consumer chưa map vào entity |
| `data.status: "ACTIVE"` | Hardcode `DRAFT` | `Contract.status` | ❌ WRONG | Consumer ghi đè thành DRAFT |
| `data.contractType: "Dịch vụ IT"` | Set `Contract.contractType` | `Contract.contract_type` | ✅ OK | Mapping đúng |
| `data.title: "Hợp đồng..."` | Set `Contract.title` | `Contract.title` | ✅ OK | Mapping đúng |
| `data.tags: ["IT", "Software"]` | Join thành string, set `Contract.tags` | `Contract.tags` | ✅ OK | Array → String, mapping đúng |
| **PARTIES DATA** |||||
| `data.parties[].name` | `ContractParty.partyName` | `contract_parties.party_name` | ✅ OK | Lưu collection riêng |
| `data.parties[].role` | `ContractParty.partyType` | `contract_parties.party_type` | ✅ OK | Mapping đúng |
| `data.parties[].representative` | `ContractParty.contactPerson` | `contract_parties.contact_person` | ✅ OK | Mapping đúng |
| `data.parties[].taxCode` | `ContractParty.taxCode` | `contract_parties.tax_code` | ✅ OK | camelCase → snake_case |
| `data.parties[].contact` | `ContractParty.phone` | `contract_parties.phone` | ✅ OK | Field name khác nhưng ý nghĩa đúng |
| `data.parties[].address` | `ContractParty.address` | `contract_parties.address` | ✅ OK | Đã bổ sung mapping |
| `data.parties[].businessLicense` | `ContractParty.businessLicense` | `contract_parties.business_license` | ✅ OK | Đã bổ sung field và mapping |
| **CONTRACT DETAILS** |||||
| `data.object: "Dịch vụ..."` | Converter → `object` | `Contract.contract_object` | ⚠️ LOG-ONLY | Service chỉ log, chưa set field |
| `data.effectiveDate: "2024-01-01"` | Converter → `effective_date` | `Contract.effective_date` | ⚠️ LOG-ONLY | Service chỉ log, chưa set field |
| `data.term: "12 tháng"` | Converter → `term` | `Contract.contract_term` | ⚠️ LOG-ONLY | Service chỉ log, chưa set field |
| `data.terminationConditions` | Converter → `termination_conditions` | `Contract.termination_conditions` | ⚠️ LOG-ONLY | Service chỉ log, chưa set field |
| **PAYMENT DETAILS** |||||
| `data.paymentDetails.totalValue: 500000000` | Converter → `total_value` | `Contract.total_value` | ⚠️ LOG-ONLY | Service chỉ log, chưa set field |
| `data.paymentDetails.schedule` | Converter → `schedule` | `Contract.payment_schedule` | ⚠️ LOG-ONLY | Service chỉ log, chưa set field |
| `data.paymentDetails.currency: "VND"` | Converter → `currency` | `Contract.currency` | ⚠️ LOG-ONLY | Service chỉ log, chưa set field |
| `data.paymentDetails.paymentMethod` | Converter → `payment_method` | `Contract.payment_method` | ⚠️ LOG-ONLY | Service chỉ log, chưa set field |
| **CLAUSES** |||||
| `data.keyClauses[].name/description/source` | Gọi `createOrUpdateContractClause` | Không có entity/table | ❌ NO-ENTITY | Chỉ log, chưa có table lưu |
| `data.favorableClauses[].clauseName` | Converter → `clause_name` | Không có entity/table | ❌ NO-ENTITY | Chỉ log, chưa có table lưu |
| `data.unfavorableClauses[].riskTo` | Converter → `risk_to` | Không có entity/table | ❌ NO-ENTITY | Chỉ log, chưa có table lưu |
| **ADVANCED FEATURES** |||||
| `data.reminders[].type/date/content` | Gọi `createOrUpdateContractReminder` | Không có entity/table | ❌ NO-ENTITY | Chỉ log, chưa có table lưu |
| `data.riskAssessment.riskLevel` | Gọi `createOrUpdateContractRiskAssessment` | `Contract.risk_level` (partial) | ⚠️ PARTIAL | Chỉ lưu riskLevel, thiếu factors/measures |
| `data.riskAssessment.riskFactors[]` | Log only | Không có field | ❌ NO-FIELD | Chưa có field lưu array factors |
| `data.riskAssessment.mitigationMeasures[]` | Log only | Không có field | ❌ NO-FIELD | Chưa có field lưu array measures |
| `data.complianceStatus.status` | Gọi `createOrUpdateContractComplianceStatus` | `Contract.compliance_status` (partial) | ⚠️ PARTIAL | Chỉ lưu status, thiếu issues/recommendations |
| `data.complianceStatus.issues[]` | Log only | Không có field | ❌ NO-FIELD | Chưa có field lưu array issues |
| `data.complianceStatus.recommendations[]` | Log only | Không có field | ❌ NO-FIELD | Chưa có field lưu array recommendations |
| **METADATA** |||||
| `metadata.serviceVersion` | Đọc được | Không lưu | ✅ OK | Event metadata, không cần persist |
| `metadata.region` | Đọc được | Không lưu | ✅ OK | Event metadata, không cần persist |

## Yêu cầu thực hiện:

### 1. **CRITICAL - Sửa ngay lập tức:**
- Set `contractNumber` từ event thay vì để trống
- Set `status` từ event thay vì hardcode `DRAFT`
- Implement thật các method `updateContractDetails` để lưu object, effectiveDate, term, terminationConditions

### 2. **HIGH - Ưu tiên cao:**
- Implement thật `createOrUpdateContractPayment` để lưu payment details vào Contract entity
- Bổ sung các field còn thiếu trong Contract entity: `contract_object`, `effective_date`, `contract_term`, `termination_conditions`, `total_value`, `payment_schedule`, `currency`, `payment_method`

### 3. **MEDIUM - Tạo entities mới:**
- Tạo `ContractClause` entity để lưu keyClauses, favorableClauses, unfavorableClauses
- Tạo `ContractReminder` entity để lưu reminders
- Tạo `ContractRiskAssessment` entity để lưu riskAssessment chi tiết
- Tạo `ContractComplianceStatus` entity để lưu complianceStatus chi tiết

### 4. **LOW - Cải thiện:**
- Bổ sung audit trail cho actor information
- Tối ưu hóa mapping logic để giảm thiểu chuyển đổi field names

Hãy thực hiện theo thứ tự ưu tiên và đảm bảo tất cả dữ liệu từ AI service được lưu trữ đầy đủ trong database.
```

## Mục đích

Lưu trữ prompt để thực hiện nhanh việc điều chỉnh mapping và persistence cho ContractSummaryPublished event, đảm bảo tính nhất quán và đầy đủ dữ liệu giữa AI service và Contract service.

## Cách sử dụng

Copy nội dung prompt trên và paste vào chat AI để thực hiện nhanh việc điều chỉnh hệ thống theo bảng mapping đã phân tích.
