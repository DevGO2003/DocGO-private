'use client'

// This file re-exports the page from the route group to enable URL mapping
// URL: /repositories/:id/files → app/(repositories)/[id]/files/page.tsx

export { default } from '../../../(repositories)/[id]/files/page'

