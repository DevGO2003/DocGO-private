fix: technical issues in event payloads - wordCount, extractedText, OCR details

FIXED ISSUES:
1. ✅ wordCount calculation
   - OLD: wordCount = null
   - NEW: wordCount = len(plaintext.split())

2. ✅ plaintext vs extractedText
   - OLD: Cả 2 đều giống nhau
   - NEW: plaintext = raw, extractedText = cleaned (remove excess whitespace)

3. ✅ OCR enhanced fields
   - Added: engine, confidence, processedAt, processingTime, error
   - Added: metadata{language, pageCount, boxCount, averageConfidence}

4. ✅ extraction details section
   - Added: extraction{status, method, extractedAt, characterCount, wordCount, error}

5. ✅ summarization details section
   - Added: summarization{status, model, processedAt, processingTime, inputTokens, outputTokens, error}

COMPLIANCE:
- All changes match document-management-sample.json schema
- characterCount: 0 instead of null when no text
- wordCount: 0 instead of null when no text
- OCR metadata structure matches sample
