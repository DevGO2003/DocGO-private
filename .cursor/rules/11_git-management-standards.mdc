---
id: "rule-git-management"
name: "Chuẩn hóa Git Management"
description: "Quy tắc quản lý Git, .gitignore, remote và workflow push"
version: "1.0.0"
alwaysApply: false
enabled: true
priority: 81
tags: ["git", "remote", "workflow"]
globs: []
excludeGlobs: []
scope: "project"
services: ["*"]
appliesTo: ["chat", "code"]
match: ""
variables: {}
rules: []
examples: []
references: []
commands: []
owners: ["devgo2003"]
createdAt: "2025-09-23T00:00:00Z"
updatedAt: "2025-09-23T00:00:00Z"
notes: "Tải theo yêu cầu để giảm context"
language: vi
---

# Git Management Standards

## Quy tắc .gitignore

### **MỌI microservice PHẢI có file `.gitignore`** tại thư mục gốc của service.

**KHÔNG BAO GIỜ commit** các file sau vào Git:
- File môi trường chứa secrets: `.env`, `.env.local`, `.env.production`
- File build/compiled: `__pycache__/`, `*.pyc`, `*.pyo`, `*.pyd`, `target/`, `build/`, `dist/`, `.next/`, `node_modules/`
- File log: `*.log`, `logs/`, `*.out`
- File IDE/Editor: `.vscode/`, `.idea/`, `*.swp`, `*.swo`, `*~`
- File OS: `.DS_Store`, `Thumbs.db`, `desktop.ini`
- File tạm thời: `*.tmp`, `*.temp`, `*.bak`, `*.backup`
- File database: `*.db`, `*.sqlite`, `*.sqlite3`
- File upload/temp: `uploads/`, `temp/`, `tmp/`, `results/`

### Quy tắc .gitignore theo loại service:

#### **Python (FastAPI)**
```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
.venv/
.env/

# Environment variables
.env
.env.local
.env.production

# Logs
*.log
logs/

# Testing
.pytest_cache/
htmlcov/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Project specific
results/
uploads/
temp/
tmp/
```

#### **Java (Spring Boot)**
```gitignore
# Compiled class file
*.class

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# Virtual machine crash logs
hs_err_pid*

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# Gradle
.gradle/
gradle-app.setting
!gradle-wrapper.jar
!gradle-wrapper.properties

# Environment variables
.env
.env.local
.env.production

# IDE
.vscode/
.idea/
*.iml
*.ipr
*.iws

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
logs/
*.log

# Temporary files
*.tmp
*.temp
*.bak
*.backup
```

#### **Node.js/Next.js**
```gitignore
# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js
.next/
out/
build/
dist/

# Environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local
.env

# Logs
*.log
logs/

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
.nyc_output

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Temporary files
*.tmp
*.temp
*.bak
*.backup
```

### Quy tắc bảo mật:
- **KHÔNG BAO GIỜ commit** file `.env` chứa secrets thật
- **LUÔN** commit file `.env.example` làm template
- **KIỂM TRA** `.gitignore` trước mỗi commit để đảm bảo không có file nhạy cảm
- **SỬ DỤNG** `.gitignore` global cho workspace nếu cần thiết

## Quản lý Remote Repository

### **Remote chính**: `origin` → `https://github.com/DevGO2003/DocGO` (public, không chứa secrets)
### **Remote private**: `private` → `https://github.com/DevGO2003/DocGO-private` (chứa secrets, file nhạy cảm)

### **Quy tắc sử dụng**:
- `origin`: Code nguồn, cấu hình cơ bản, tài liệu công khai
- `private`: API keys, database credentials, file nhạy cảm, logs, build artifacts

### **Cách thêm remote private**:
```bash
git remote add private https://github.com/DevGO2003/DocGO-private.git
git remote -v  # Kiểm tra remotes
```

## Quy tắc đồng bộ hóa tự động

- **Khi push lên `origin`**: Tự động đồng bộ sang `private` để đảm bảo cả hai remote luôn có code mới nhất
- **Khi push lên `private`**: Chỉ push secrets và file nhạy cảm, không đồng bộ ngược về `origin`

## Quy trình push và đồng bộ hóa

### **1. Push code công khai (khuyến nghị)**:
```bash
# Bước 1: Push lên origin
git add .  # Code nguồn, không có secrets
git commit -m "feat: thêm tính năng mới"
git push origin main

# Bước 2: Tự động đồng bộ sang private (đồng bộ code nguồn)
git push private main
```

### **2. Push code nhạy cảm**:
```bash
# Chỉ push file nhạy cảm lên private
git add .env  # File chứa secrets
git commit -m "feat: thêm cấu hình môi trường"
git push private main  # KHÔNG push lên origin
```

### **3. Đồng bộ hóa thủ công (nếu cần)**:
```bash
# Đồng bộ từ origin sang private (để lấy code mới nhất)
git fetch origin
git checkout main
git pull origin main
git push private main

# Đồng bộ từ private sang origin (chỉ khi cần thiết)
git fetch private
git checkout main
git pull private main
git push origin main
```

## Quy tắc bảo mật khi đồng bộ hóa

- **KHÔNG BAO GIỜ** push file `.env` chứa secrets thật lên `origin`
- **LUÔN** kiểm tra `.gitignore` trước khi push để đảm bảo không có file nhạy cảm
- **Ưu tiên** push lên `origin` trước, sau đó đồng bộ sang `private`
- **Kiểm tra** trạng thái remote trước khi push để tránh conflict

## Script tự động hóa đồng bộ hóa

### **PowerShell Script**
```powershell
# Script push và đồng bộ hóa tự động
function git_push_and_sync {
    param(
        [Parameter(Mandatory=$true)]
        [string]$commit_message,
        
        [Parameter(Mandatory=$false)]
        [string]$branch = "main"
    )
    
    # Commit changes
    git add .
    git commit -m $commit_message
    
    # Push to origin
    Write-Host "Pushing to origin..." -ForegroundColor Green
    git push origin $branch
    
    # Sync to private
    Write-Host "Syncing to private..." -ForegroundColor Green
    git push private $branch
    
    Write-Host "Push and sync completed successfully!" -ForegroundColor Green
}

# Sử dụng: git_push_and_sync "feat: thêm tính năng mới"
```

### **Bash Script**
```bash
#!/bin/bash

git_push_and_sync() {
    local commit_message="$1"
    local branch="${2:-main}"
    
    # Commit changes
    git add .
    git commit -m "$commit_message"
    
    # Push to origin
    echo "Pushing to origin..."
    git push origin "$branch"
    
    # Sync to private
    echo "Syncing to private..."
    git push private "$branch"
    
    echo "Push and sync completed successfully!"
}

# Sử dụng: git_push_and_sync "feat: thêm tính năng mới"
```

## Git Workflow Best Practices

### **Branch Strategy**
- `main`: Production-ready code
- `develop`: Integration branch for features
- `feature/*`: Feature branches
- `hotfix/*`: Hotfix branches for production issues

### **Commit Message Convention**
```
feat: thêm tính năng mới
fix: sửa lỗi
docs: cập nhật tài liệu
style: format code
refactor: refactor code
test: thêm test
chore: cập nhật dependencies
```

### **Pre-commit Hooks**
```yaml
# .pre-commit-config.yaml
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict
```

## Git Security Checklist

### **Trước mỗi commit**:
- [ ] Kiểm tra `.gitignore` có đầy đủ không
- [ ] Không có file `.env` chứa secrets
- [ ] Không có file nhạy cảm (API keys, passwords)
- [ ] Commit message rõ ràng và có ý nghĩa

### **Trước mỗi push**:
- [ ] Code đã được test
- [ ] Không có conflict với remote
- [ ] Push đúng remote (origin vs private)
- [ ] Backup code quan trọng

### **Sau mỗi push**:
- [ ] Kiểm tra CI/CD pipeline
- [ ] Verify code trên remote repository
- [ ] Thông báo team về thay đổi lớn

---

**Lưu ý**: Tất cả Git management phải tuân thủ quy tắc này để đảm bảo tính bảo mật và workflow hiệu quả.